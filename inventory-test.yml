config:
  target: "http://localhost:8080"  # API 서버 URL
  phases:
    - duration: 30       # 30초 동안
      arrivalRate: 10    # 초당 50개의 요청으로 시작
      rampTo: 50
      name: Warm Up
    - duration: 300       # 30초 동안
      arrivalRate: 50   # 초당 200개의 요청 유지
      rampTo: 300        # 초당 500개의 요청까지 다시 증가
      name: Ramp Up Load

scenarios:
  - flow:
      - post:
          url: "/orders"  # API 엔드포인트
          headers:
            Content-Type: "application/json"  # JSON 형식 요청
            Authorization: "Bearer eyJhbGciOiJIUzI1NiJ9.eyJ1c2VySWQiOjIsImlhdCI6MTczMjQ0OTk4NiwiZXhwIjoxNzMyNTM2Mzg2fQ.oWKZpAbD0kL91qF_JOCUCMWoQfdQiwTaRu0Jaw7TiZM"
          json:
            infos:
              - itemId: 1
                quantity: 1
